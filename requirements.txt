# Requirements for FoodID
# -----------------------
# Pinned dependencies for running the application and for local development/testing.
# Pinning versions ensures reproducible installs across machines and CI.
#
# Install into a virtual environment:
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1   # PowerShell (Windows)
#   source .venv/bin/activate      # macOS / Linux
#   pip install -r requirements.txt

# --- Runtime dependencies (required to run the app) ---
flask==3.0.3           # WSGI web framework providing the API
requests==2.32.3       # HTTP client used by the Gradio UI
gradio==4.44.0         # Web UI framework for frontend CRUD operations

# --- Development / test dependencies ---
pytest==8.3.3          # Unit test runner for backend and UI functions

# --- Optional dependencies (comment/uncomment as needed) ---
# gunicorn==21.2.0      # Recommended WSGI server for production (Linux/macOS)
# waitress==2.1.2       # Alternative WSGI server for Windows users
# python-dotenv==1.0.0  # Load `.env` files automatically in development

# --- Platform-conditional dependencies ---
# Conditional backport for audio on Python >= 3.13
audioop-lts; python_version>='3.13'

# Notes
# - For larger projects, consider a separate `requirements-dev.txt` for tooling
#   (linters, formatters, test extras). For small projects this single file is fine.
